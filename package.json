{
  "name": "classesjs",
  "version": "1.0.0",
  "description": "Homework project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server",
    "api": "json-server dane.json --watch"
  },
  "author": "LK",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.12.1"
  }
}
